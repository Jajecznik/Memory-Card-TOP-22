function Header() {
  return (
    <header className="flex justify-between p-4">
      <div>
        <p>MEMORY CHECKER</p>
        <p>To get a point click on card but just once!</p>
      </div>
      <div>
        <p>Your score:</p>
        <p>Your best:</p>
      </div>
    </header>
  );
}

export default Header;